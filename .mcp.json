{
  "mcpServers": {
    "postgres": {
      "command": "uvx",
      "args": ["--python", "3.12", "--with", "mcp<2.0.0", "postgres-mcp", "--access-mode=unrestricted"],
      "env": {
        "DATABASE_URI": "${CAPYBOOKS_DB_URI}"
      }
    }
  }
}
